public class computer {
    Distribute obj = new Distribute();
    String[] cards = new String[8];
    // cc=obj.computer_cards[0];
    void makeDecision()
    {
        cards=obj.computer_cards;
        System.out.println(cards);
    }
}
